// Shared request/response DTO types
// Model DB (User, Booking, Patient, dst) TIDAK perlu ditulis manual —
// sudah auto-generated oleh Prisma Client dari schema.prisma,
// tinggal import { Booking } from '@prisma/client'

// DTOs will be added as endpoints are built
export {};
